section.hero
    .hero__container  
        .hero__content.fadeInLeft 
            p.hero__subtitle
                //span.hero__subtitle--badge Try it now
                span Integration platform for developers

            h1.hero__title Build awesome integrations for your application without the hassle
            p.hero__text
                | Friends don’t let friends build integrations alone. Our code-first integration platform and SaaS connectors remove the headaches of dealing with different APIs and eliminate the operational burden when running at scale.
            a.hero__cta.sign-up-cta(onclick="sendEvent('CTA', 'Clicked hero button');") 
                | Join waitlist
                svg(viewBox="0 0 10 18" fill="none" xmlns="http://www.w3.org/2000/svg")
                    path(d="M1 1L9 9L1 17" stroke-width="2" stroke-linecap="round" stroke-linejoin="round")


        .hero__images.fadeInRight
            != filters.getImageTag(`homepage/hero/hero--illustration.png`, 'Integration platform for developers', '(max-width: 2000px) 50vw, 100vw', {}, [1700, 2550])
